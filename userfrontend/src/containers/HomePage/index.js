import React from 'react'
import Header from '../../components/Header';
import MenueHeader from '../../components/MenuHeader';
//routes for Header and MenuHeader in userfrontend
const HomePage = (props) => {
    return(
        <div>
            <Header/>
            <MenueHeader/>
            
        </div>
    )
}

export default HomePage;